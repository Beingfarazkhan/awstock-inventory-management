import { Metadata } from "next";
import Page from "./page"; // import your Demo's page

export const metadata: Metadata = {
  title: "Settings",
  description:
    "Configure your account preferences, notifications, and app settings.",
};
export default Page;
