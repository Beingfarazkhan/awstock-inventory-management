import { Metadata } from "next";
import Page from "./page";

export const metadata: Metadata = {
  title: "Dashboard",
  description:
    "Overview of your inventory, recent activities, and key metrics at a glance.",
};
export default Page;
