import { Metadata } from "next";
import Page from "./page"; // import your Demo's page

export const metadata: Metadata = {
  title: "Users",
  description:
    "Manage user accounts, roles, and permissions for your inventory system.",
};

export default Page;
